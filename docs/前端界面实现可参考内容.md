以下是所有文件的完整内容：

---

## 1. `MainWindow.fxml`（共 241 行，已全部读取）

```1:241:miduo-frontend/src/main/resources/fxml/MainWindow.fxml
<?xml version="1.0" encoding="UTF-8"?>
...
<BorderPane fx:controller="com.miduo.cloud.frontend.controller.MainController" ...>
  <!-- 顶部：MenuBar + ToolBar -->
  <!-- 中间：左侧生产信息面板 + 中间数据接收区 + 右侧统计与任务控制面板 -->
  <!-- 底部：状态栏 -->
</BorderPane>
```

**结构摘要：**
- **顶部**：菜单栏（文件/配置/数据/帮助）+ 工具栏（任务管理、数据上传、查询码、码替换、清屏）
- **左侧面板**（prefWidth=340）：当前生产信息（生产单号、产品编号/名称/规格、采集规格、计划数量、生产批次、类型）+ 实时上传数据区 ListView
- **中间面板**（hgrow=ALWAYS）：数据接收区 ListView + 操作日志 TextArea + 报警信息 TextArea
- **右侧面板**（prefWidth=336）：单位实时统计（当前箱数、每垛箱数、已生产垛数、完成度）+ 任务控制按钮组（启用任务、订单结单、读码剔除、添加码、清除码、指定当前箱数、强制满垛、删除本垛无码、关闭报警）
- **底部状态栏**：设备状态、工作状态、许可证状态、当前时间、版本号

---

## 2. `base-styles.css`（共 140 行，已全部读取）

```1:140:miduo-frontend/src/main/resources/css/base-styles.css
/* 导入：theme-colors.css, typography.css, button-styles.css,
          input-styles.css, table-styles.css, status-styles.css */

.root { -fx-font-family: "Microsoft YaHei"; -fx-font-size: 14px; -fx-background-color: #f5f5f5; }

/* 菜单栏、工具栏、状态栏、对话框、分页、滚动条、分隔符、空状态、数据接收列表 */
```

**要点：**
- 全局字体：`Microsoft YaHei`，14px
- 该文件作为入口，通过 `@import` 引入其他 6 个 CSS 文件
- 定义了 `.menu-bar`、`.tool-bar`、`.status-bar`、`.dialog-pane`、`.data-receive-list` 等公共样式
- 滚动条使用透明背景 + 圆角 thumb 设计

---

## 3. `theme-colors.css`（共 62 行，已全部读取）

```1:62:miduo-frontend/src/main/resources/css/theme-colors.css
.root {
    -primary-color: #4a90e2;      /* 主蓝色 */
    -success-color: #28a745;      /* 绿色 */
    -error-color:   #dc3545;      /* 红色 */
    -warning-color: #ffc107;      /* 橙黄 */
    -info-color:    #17a2b8;      /* 青色 */
    -text-primary:  #333333;
    -border-color:  #e0e0e0;
    -surface-color: #f8f9fa;
    -hover-color:   #f0f0f0;
}
/* .application-background、.panel、.card */
```

**要点：**
- 定义了全套 CSS 变量（主色、辅助色、状态色、中性色）
- `.panel`：白底 + 1px 边框 + 15px 内边距
- `.card`：白底 + 圆角 + 阴影投影

---

## 4. `button-styles.css`（共 116 行，已全部读取）

```1:116:miduo-frontend/src/main/resources/css/button-styles.css
.primary-button   { -fx-background-color: -primary-color; -fx-text-fill: white; ... }
.secondary-button { -fx-background-color: transparent; -fx-border-color: -primary-color; ... }
.danger-button    { -fx-background-color: -error-color;   -fx-text-fill: white; ... }
.success-button   { -fx-background-color: -success-color; -fx-text-fill: white; ... }
.toolbar-button   { -fx-min-height: 70px; -fx-content-display: graphic-only; ... }
```

**要点：**
- 定义了 5 种按钮样式：主要、次要、危险、成功、工具栏
- 每种按钮都有 `:hover` 和 `:pressed` 状态
- 工具栏按钮高度 70px，仅显示图标（graphic-only），图标下方通过 FXML 中的 VBox+Label 叠加文字

---